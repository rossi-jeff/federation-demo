type Award {
  id: ID!
  name: String
  position: Int
  organization_id: Int
  active: Boolean
  created_at: String
  updated_at: String

  Organization: Organization
}

extend type Query {
  awards: [Award]
  award(id: ID!): Award
}
