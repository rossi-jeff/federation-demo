overwrite: true
schema: "services/**/*.schema.graphql"
documents: null
generates:
  generated/graphql.ts:
    config:
      federation: true
    plugins:
      - "typescript"
      - "typescript-resolvers"
config:
  mappers:
    Activity: "../services/activities/src/activities/types#ActivityType"
